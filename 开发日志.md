# 📅 GalRec 开发进度日志

**日期**: 2026-01-17
**版本**: v1.1.0
**里程碑**: Non-linear Gameplay Support (非线性剧情支持闭环)

## ✨ 核心特性更新 (Features)

1. **Step 3 (Play) 分支处理支持**

- 实现了 `play.branches` 协议，支持在常规剧情推进中配置“例外/分支”检测。
- 引入了“线性消费队列”模式：Runner 在检测到分支选项并点击后，会自动将该选项从待执行列表中移除，防止重复触发。
- **UI 更新**: 在 TaskEditor 的 Step 3 区域集成了拖拽排序列表（复用 Step 2 组件），支持截图录入分支条件。

2. **Runner 智能检测策略 (Smart Scan)**

- 放弃了高开销的每帧全量检测，采用 **串行嵌入式 (Serial Embedded)** 逻辑。
- 实现了 **A/B 混合策略**：
- **策略 A (Head Check)**: 优先检测队列首个分支（高频，极低开销）。
- **策略 B (Deep Scan)**: 首个未命中时，扫描剩余所有分支（兜底容错）。

3. **系统稳定性增强 (Stability)**

- **F9 全局熔断**: 新增独立线程监听 `F9` 热键，任何时候按下均可强制终止 Runner 并恢复 UI 窗口。
- **错误自毁**: Runner 遇到文件冲突或严重 IO 错误时会自动退出，避免陷入无限重试死循环。

## 🛠️ 技术细节 (Technical Details)

- **UI/Schema**: 复用了 `SortableActionItem` 组件，保持了 Step 2 和 Step 3 的体验一致性。
- **Runner**: 重构了 `play_mode_default`，将分支检测逻辑注入到 `wait_pacing` 与 `advance` 之间，利用 Pacing 的天然时延作为节流阀，确保了极低的 CPU 占用率。
- **API**: 完善了 `templates` 路由，支持 ROI 裁剪与 Frame 缓存机制。

# 📌 GalRec 项目上下文断点（2026-01-17）

## 一、当前里程碑：v1.1 分支处理闭环

**核心成就**：
在 v1.0 MVP（基础录制）的基础上，完成了 **Step 3 (Play) 的分支/异常处理能力**。现在 Runner 可以一边推进剧情，一边“瞥视”屏幕，当检测到选项或弹窗时优先点击处理，从而支持非线性录制。

## 二、功能完成度状态

### 1️⃣ UI 端 (Electron/React)

- **Games/Config/Templates**：已稳定，无需变动。
- **TaskEditor (任务编辑器)**：
- ✅ **截图/ROI 工具**：已集成 `CaptureModal` 和 `PointPicker`，支持直接从游戏窗口截取模板或拾取坐标。
- ✅ **Step 2 (Pre-play)**：支持拖拽排序，支持 Action 序列。
- ✅ **Step 3 (Play)**：
- 支持配置 `Advance Method` (Mouse/Key) 和 `Pacing`。
- 支持 **覆盖点击坐标 (Target Override)**，并集成了坐标拾取器。
- **[NEW] Branching 列表**：复用了 Step 2 的 `SortableActionItem` 组件，支持拖拽添加分支检测项（Template + Delay）。虽然 `Delay` 字段在分支中作用有限，但为了复用组件保持现状。

### 2️⃣ 后端 & 执行器 (Python/Runner)

- **稳定性**：
- ✅ **F9 全局熔断**：独立线程监听 F9，强制停止录制并恢复 UI。
- ✅ **自我终结**：遇到文件冲突或严重错误自动退出，防止死循环。

- **核心逻辑 (Play Loop)**：
- ✅ **串行嵌入式决策**：在主循环的 `wait_pacing` 之后、`advance` 之前插入分支检测。
- ✅ **智能 A/B 策略**：
- **策略 A**：优先检测队列首个分支（高频）。
- **策略 B**：首个未中时，扫描剩余分支（兜底）。

- ✅ **队列消费**：匹配命中后，自动移除该项及之前的所有项，防止回滚。

- **Schema 对齐**：
- `play.branches`: List of `{ template: string, delay: float }`。
- `vision.action_template_thr`: 全局阈值透传。

## 三、代码关键变更点（记忆唤醒）

下次开始时，只需知道以下文件是**最新且已验证**的：

1. **`runner.py`**：

- `play_mode_default` 函数被重写，包含了分支检测逻辑。
- `play_dispatch` 和 `run_all_sequences` 增加了 `act_thr` 参数透传。

2. **`TaskEditor.tsx`**：

- Step 3 区域增加了 `<DndContext>` 包裹的分支编辑列表。
- 增加了 `handleDragEndStep3` 处理分支排序。

3. **`api/templates.py`**：

- 包含了 `capture_frame` / `crop_save` / `capture_roi` 等一系列截图辅助接口。

## 四、遗留问题与决策 (Known Decisions)

1. **UI 组件复用**：Step 3 的分支编辑复用了 Step 2 的组件，保留了 `Delay` 输入框。**决策**：虽然 Delay 在分支场景用处不大，但为了代码复用且不破坏 Step 2 功能，予以保留，Runner 默认处理即可。
2. **分支风险**：目前逻辑是“点击即消费（删除）”。**决策**：暂不处理“点击未响应导致卡关”的边缘情况，保持逻辑简单，相信 OpenCV 的匹配准确率。

## 五、下一步计划 (Next Steps)

**优先级：最高**

- **Audio Pacing (音频驱动)**
- **目标**：解决 `pacing: system` 固定延时无法适配语音长度的问题。
- **技术路径**：引入音频监听库（`soundcard` + `numpy`），计算 RMS 能量值，实现“语音结束 -> 触发点击”。
