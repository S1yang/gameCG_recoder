# 日志

## 1）我们现在 UI/工程做到哪里（你可以快速核对）

### 已闭环（能用、能跑、能保存、能恢复）

- **GamesTab**

  - 列表/详情、Set Active、Open Folder
  - ✅ 新建/删改/导入恢复 registry（你已实现）
  - ✅ “删除只是删 registry 记录” + “可从文件夹导入恢复”这一套已落地

- **ConfigTab**

  - ✅ 从 game.yaml / config.yaml 读取 + 可编辑写回
  - ✅ 参数解释型表单（你提的方向）
  - ✅ 默认模板/快速复用（你提的方向）

- **TemplatesTab**

  - 能看到模板列表（目前是“存放截图资源”的定位）

- **TasksTab + TaskEditor**

  - ✅ 任务选择/编辑保存闭环
  - ✅ 拖拽排序（你已做并验证）
  - ✅ 批量标记完成/未完成（你已做并验证）
  - ✅ 批量 run（通过写 progress.run_only_batch / clear_run_only + /runner/start 触发）已可用

- **Runner 闭环（关键）**

  - ✅ /runner/start 能拉起 runner（Windows 控制台保留与否可控 + log_to_file）
  - ✅ 通过 `GALREC_GAME_ROOT` 注入 active game root，runner 能正确找 game.yaml/config.yaml/sequences/templates
  - ✅ runner 运行前激活游戏窗口，结束前恢复 UI 前景
  - ✅ 你给的日志证明 A/B/C/D 全链路正常

---

## 2）我们这个工具的目标（“你之前说的 + 我之前说的”合并版）

### 用户目标（从“能录制”到“低成本批量录制”）

1. 管理多个游戏的录制工程（每个 game 是独立工作区：config、sequences、templates、output）
2. 用“任务序列（Step1/2/3）”把一个 CG 录制过程配置出来
3. 以最少的手工操作批量跑完任务（全跑/选跑/按顺序跑）
4. 出错可恢复、可重跑、可追踪（progress 状态、日志、产物路径）

### 工程目标（稳定性/一致性）

- active game 是单一入口：UI 选 active → API/Runner 全跟随
- 路径与文件组织稳定：registry 相对路径、game root 注入、runner 全从 game root 读写
- 任务/队列/状态的一致性：Tasks 列表、queue 顺序、progress 状态、runner 行为互相对齐

---

## 3）接下来要做什么（更完整的重点清单，含你提到的 + 我补的）

### A. TaskEditor 工具链（你明确说的痛点）

- **截图捕获/ROI 工具**（Step1/Step2 里要能直接截图保存到 templates，并写入 YAML 引用）
- **模板缩略图/预览**（列表/step 卡片旁展示缩略图；缺图时提示）
- **拖拽排序扩展到 Step2 序列维护**（你提到的覆盖）

### B. Step3 播放能力升级（你提的 3 条）

- **分支选项**：出现选择时靠模板匹配跳转/点击
- **时间序列动作复用**：把 Step2 的 action list 机制复用到 Step3
- **audio pacing 真正落地**：不仅是字段，占用 runner 的 pacing 分支实现

### C. 状态系统优化（你说“状态显示/管理还要优化”这个很关键）

- 统一 status 枚举与颜色/标签
- 批量操作后的状态一致性（刷新策略、乐观更新/回滚）
- 快捷过滤/视图：只看未完成、只看 failed、只看本次 run 集合
- “重跑策略”：reset / mark undone / clear error / clear output（规则要定清楚）

### D. Runner 可观测性与可控性（你已经能跑，但还可以更像“工具”）

- **Runner 面板**：展示最近一次日志（tail）、运行中状态、最后一次产物
- 可控项：stop/pause（如果你愿意做），至少 stop
- 失败定位：把常见错误归因（缺 template、阈值太高、窗口找不到、OBS 未连接）

### E. 配置体验继续下沉（你认同 ConfigTab 是“交互填写字段表”）

- Default config template：一键设为默认 → 新建 game 时复用
- 字段校验：game_window_title 必填、obs 端口/密码校验、阈值范围校验
- “从 runner 角度解释参数”（你说“以 runner 为准”）：ConfigTab 的说明应完全映射 runner 中读取的 cfg

---

## 4）下一步我们怎么排（给你一个不啰嗦但可执行的顺序）

结合你偏好的“闭环一个再下一个”，以及你目前最痛的点，我建议：

1. **TaskEditor：截图/ROI + 缩略图**（立刻提升配置效率）
2. **Step2 序列维护：拖拽排序扩展**（一致性交互）
3. **状态系统优化**（不然任务一多就不可控）
4. **Step3 两条主线：分支/动作序列 → 再做 audio pacing**（功能升级）

---

# 断点

好，下面是一份**可直接复制到新对话中使用的「上下文断点（Context Checkpoint）」**。
我已**去掉“MVP”表述**，并严格以你现在已经实现并验证过的真实状态为准整理。

---

# 📌 GalRec 项目上下文断点（2026-01-15）

## 一、项目总体目标（当前共识）

GalRec 是一个 **面向 Galgame / 视觉小说类游戏的 CG 自动录制工具**，核心目标是：

- 以 **“游戏工程（Game）”** 为基本单位，管理配置、模板、任务与产出
- 通过 **可配置的任务序列（Task / Sequence）**，自动完成 CG 进入、推进、结束检测与录制
- 支持 **批量录制、失败可恢复、状态可追踪**
- UI 负责“配置 / 调度 / 管理”，Runner 负责“稳定执行”

---

## 二、当前系统架构（已稳定）

### 1️⃣ 技术架构

- **前端**：Electron + Vite + React + TypeScript + shadcn/ui
- **后端**：FastAPI（本地）
- **执行器**：`runner.py`（独立进程，由 FastAPI 拉起）
- **通信**：

  - UI → FastAPI：HTTP
  - FastAPI → Runner：`subprocess.Popen`

- **路径管理核心**：

  - registry.yaml（游戏注册表）
  - active game → 通过 `GALREC_GAME_ROOT` 注入 runner

---

## 三、核心概念与目录结构（已落地）

### Game（游戏工程）

- 每个 game 是一个**独立工作区**
- 包含：

  - `game.yaml` / `config.yaml`（运行配置）
  - `sequences/*.yaml`（任务定义）
  - `templates/*.png`（截图模板）
  - `output/`（录制产物）

### Registry

- `registry.yaml` 只记录：

  - key / title / root / window_title

- 删除 Game **只删 registry 记录**
- 已支持：**从文件夹导入 → 恢复 registry**

---

## 四、UI 当前完成状态（已验证）

### ✅ GamesTab（已闭环）

- 游戏列表 / 详情
- Set Active / Open Folder
- 新建 / 删除 / 编辑
- **导入文件夹恢复 registry**
- 校验 window_title / root
- 不需要再拆分

### ✅ ConfigTab（已闭环）

- 从 `game.yaml / config.yaml` 读取并写回
- 交互式字段填写（而非 raw yaml）
- 参数解释 **以 runner 实际读取逻辑为准**
- 支持作为“默认模板”复用（方向已明确）
- 本质是 Game 的延续，不单独建模

### ✅ TemplatesTab（稳定）

- 当前定位：**模板资源管理**
- templates 只是 runner 使用的素材池
- 不涉及执行逻辑

---

## 五、Tasks / TaskEditor（当前真实能力）

### ✅ TasksTab（已验证可用）

- 任务列表（来自 `sequences/*.yaml`）
- **拖拽排序（queue.yaml）**
- 单选 / 多选
- 批量操作：

  - 标记完成 / 未完成
  - 批量 run（通过 progress.run_only_batch）

- Run 行为：

  - UI 写 progress
  - UI 调 `/runner/start`
  - runner 按顺序执行

- 当前状态显示可用，但**需要进一步优化**

### ⚠️ TaskEditor（部分未完成）

已具备：

- Step1 / Step2 / Step3 结构
- Raw 编辑 & 保存
- 与 runner schema 对齐

尚需实现（重点）：

1. **截图捕获 / ROI 工具**

   - 直接生成 template
   - 自动写入 YAML

2. **模板缩略图预览**
3. **Step2 的序列支持拖拽排序**
4. **更强的可视化编辑（减少 raw）**

---

## 六、Runner 状态（关键：已完全跑通）

### Runner 启动机制（已修复）

- `/runner/start`

  - 注入 `GALREC_GAME_ROOT`
  - Windows 支持：

    - show_console
    - keep_console
    - log_to_file（`.galrec/runner.log`）

### Runner 行为（已验证）

- 启动前：

  - 解析 active game root
  - 读取 `game.yaml / config.yaml`
  - **激活游戏窗口（window_title）**

- 执行流程：

  - A：entry.template + scene diff 校验
  - B：preplay actions
  - C：play loop
  - D：end.template + diff fallback

- 结束后：

  - 停止 OBS
  - 写 progress
  - **恢复 GalRec UI 到前景**

- 日志显示完整、行为稳定（你已给出成功 log）

⚠️ 已明确：
**不要直接在命令行测试 runner.py，所有问题都应通过 UI → /runner/start 排查**

---

## 七、已确认的问题（标记为已解决）

- ❌ config.yaml 缺失 / 路径错误
  ✅ 已通过 `GALREC_GAME_ROOT` + runner 内 resolve 修复
- ❌ game.window_title / config.game_window_title 混乱
  ✅ 已统一为 config 中字段，runner 只认 config
- ❌ CMD 一闪而过
  ✅ 通过 `cmd /K` + keep_console + log_to_file 解决
- ❌ UI 前景被游戏遮挡
  ✅ runner 启动/结束均主动激活窗口

---

## 八、接下来明确要做的事（真实优先级）

### ① TaskEditor 工具增强（最高优先）

- 截图 / ROI 捕获
- 模板缩略图
- Step2 拖拽排序

### ② Step3 播放能力升级

1. **分支选项支持**

   - 用模板判断分支并点击

2. **时间序列动作**

   - 复用 Step2 的 action list

3. **Audio pacing**

   - runner 中真正实现 pacing=audio

### ③ 状态系统优化

- 统一 status 枚举与 UI 表现
- 批量操作后的状态一致性
- 失败任务的重跑/清理策略
- 视图过滤（未完成 / failed / 本次 run）

### ④ Runner 可观测性（之后）

- 最近一次运行日志查看
- 当前运行状态显示
- stop / abort 能力（若需要）

---

## 九、开发协作约定（重要）

- **以 runner 为准**：

  - UI schema / ConfigTab / TaskEditor 都必须与 runner 读取字段一致

- **先闭环再扩展**
- **不做“为了架构而架构”的拆分**
- **不再使用“MVP”表述**

---

👉 **使用方式**：
复制以上整段内容 → 粘贴到新的 ChatGPT 对话中 → 继续开发即可。
